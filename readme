So this project is basically intended to be a framework upon which to build large libraries of linguistic relational data.

This data, along with the functions and methods deveoped, will be the framework for a conversational AI. 